__pycache__/
*.py[cod]
.venv/
venv/
.env
*.zip
.ruff_cache/
var/
node_modules/
.next/
out/
.claude/settings.local.json*

# Real customer exports and the extraction records made from them. These carry
# party names, rates and amounts, and the repo is shared with judges.
data/
evals/*/records.json
frontend/node_modules
# Synthetic samples are committed deliberately (see the header in each file);
# only real customer data is excluded, which data/ above already covers.
!data/synthetic/
!data/synthetic/**
